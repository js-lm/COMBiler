#pragma once

#include <raylib.h>

#include "interface/anchors.hpp"

using namespace constants::anchors::note_canvas;

namespace constants::layout::note_canvas{

    constexpr Rectangle NoteCanvasGroupBox{NoteCanvasAnchor.x + 0, NoteCanvasAnchor.y + 0, 848, 368};

} // namespace constants::layout::note_canvas